/**
 * Mapeamento objeto-relacional (JPA): uma classe por tabela definida em
 * {@code server/database/migrations/}. Não coloque regra de negócio aqui —
 * apenas estado persistível alinhado ao DDL em SQL.
 */
package com.ufc.persistence.entity;
